import React from 'react';
import SiteStructure from './structure';

/**
 * Returns a component used to render the core of the site.
 * 
 * This component renders the site structure and global available services and contexts.
 */
const Site = () => {
    return <SiteStructure />
}

export default Site;
